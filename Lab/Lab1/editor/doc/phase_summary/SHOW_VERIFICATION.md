# Show 命令快速验证

## 修改内容总结

### ✅ 已完成的功能

1. **支持范围参数** - `show [起始行:结束行]`
   - 不带参数：显示全文
   - 带参数：显示指定范围

2. **完整的错误处理**
   - 格式错误检查（必须是 `行:列` 格式）
   - 起始行不能小于 1
   - 结束行不能超出文件大小
   - 起始行不能大于结束行
   - 行号必须是数字

3. **更新了帮助信息**
   ```
   show [起始行:结束行] - 显示文件内容（可指定范围）
   ```

## 快速验证步骤

### 步骤1：编译项目
```powershell
cd E:\_ComputerLearning\15_SoftwareDesign\Lab\Lab1\editor
mvn clean package -DskipTests
```

### 步骤2：运行编辑器
```powershell
java -jar target/editor-1.0-SNAPSHOT.jar
```

### 步骤3：测试基本功能
在编辑器中执行：
```
> init show_test.txt
> append "Line 1"
> append "Line 2"
> append "Line 3"
> append "Line 4"
> append "Line 5"

> show
(应显示全部5行)

> show 2:4
(应只显示第2-4行)

> show 1:2
(应只显示第1-2行)

> show 5:5
(应只显示第5行)
```

### 步骤4：测试错误处理
```
> show 0:3
错误: 起始行号不能小于1

> show 1:100
错误: 结束行号超出文件范围 (文件共 5 行)

> show 5:2
错误: 起始行号不能大于结束行号

> show 1-3
错误: 范围格式错误，应为 <起始行:结束行>
```

### 步骤5：验证完成
如果上述测试全部通过，说明 `show` 命令的改进已成功实现！

## 技术实现细节

### 核心代码
```java
private void cmdShow(ParsedCommand cmd) {
    // ... 前置检查 ...
    
    int startLine = 1;
    int endLine = buffer.getSize();
    
    // 解析范围参数
    if (cmd.getArgCount() > 0) {
        String range = cmd.getArg(0);
        String[] parts = range.split(":");
        
        if (parts.length != 2) {
            System.out.println("范围格式错误，应为 <起始行:结束行>");
            return;
        }
        
        startLine = Integer.parseInt(parts[0]);
        endLine = Integer.parseInt(parts[1]);
        
        // 边界检查
        // ...
    }
    
    // 显示指定范围
    for (int i = startLine; i <= endLine; i++) {
        System.out.println(i + ": " + buffer.getLine(i));
    }
}
```

### 错误处理类型
1. **格式错误** - 不符合 `start:end` 格式
2. **数值错误** - 非数字输入
3. **范围错误** - 起始 > 结束
4. **边界错误** - 超出文件行数范围

## 与文档要求对比

| 要求 | 实现状态 |
|------|---------|
| 支持 `show [start:end]` 格式 | ✅ |
| 不指定参数显示全文 | ✅ |
| 指定范围显示部分内容 | ✅ |
| 异常处理（范围超出） | ✅ |
| 格式验证 | ✅ |
| 用户友好的错误提示 | ✅ |

## 相关文件

- **源代码**: `src/main/java/top/thesumst/cli/CommandLineApp.java`
- **使用文档**: `USAGE.md`
- **测试文档**: `TEST_SHOW_COMMAND.md`
- **快速验证**: 本文件

## 下一步

完成编译后即可进行实际测试！🎉
